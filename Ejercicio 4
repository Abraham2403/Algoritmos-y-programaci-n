#include <stdio.h>
#include <stdlib.h>

int main()
{
    float *p = (float *)0x1000; // Dirección inicial dada
    printf("Direccion de p: %p\n", (void*)p);
    printf("Direccion de p + 3: %p\n", (void*)(p + 3));
    return 0;
}
